services:
  - type: web
    name: signvision-backend
    env: python
    buildCommand: pip install -r ai-model/requirements.txt flask flask-cors
    startCommand: python backend/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.15
